//! Driven ports the kernel consumes — storage, config, scheduler, telemetry.
//!
//! Populated as driven adapters land (phases 4–6 of the walking skeleton); see the
//! root `AGENTS.md` for the planned inventory.
