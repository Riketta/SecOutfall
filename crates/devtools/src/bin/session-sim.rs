//! Fake event source + fake clock harness that drives full multi-reboot study
//! simulations in milliseconds — the core regression suite (phase 4).

fn main() {
    println!("session-sim: stub (phase 4)");
}
