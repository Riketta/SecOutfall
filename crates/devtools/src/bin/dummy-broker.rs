//! Dev observer: launches a real nats-server and logs every message on the
//! configured channels — Rust replacement of legacy `DummyBroker`.
//! Implementation lands after the NATS adapter exists (phase 6).

fn main() {
    println!("dummy-broker: stub (phase 6)");
}
